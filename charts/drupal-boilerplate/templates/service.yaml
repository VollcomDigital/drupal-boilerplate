apiVersion: v1
kind: Service
metadata:
  name: {{ include "drupal-boilerplate.fullname" . }}
  labels:
    {{- include "drupal-boilerplate.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "drupal-boilerplate.selectorLabels" . | nindent 4 }}
